# CodeRabbit Configuration
# https://docs.coderabbit.ai/guides/configure-coderabbit
#
# Kopiere diese Datei in dein Projekt-Root als .coderabbit.yaml

language: "de"

reviews:
  # Review-Einstellungen
  request_changes_workflow: true
  high_level_summary: true
  poem: false
  review_status: true
  collapse_walkthrough: false

  # Auto-Review bei PRs
  auto_review:
    enabled: true
    drafts: false

  # Pfade die ignoriert werden sollen (anpassen nach Bedarf)
  path_filters:
    - "!**/*.md"
    - "!**/*.txt"
    - "!.gitignore"
    - "!LICENSE"

chat:
  auto_reply: true
